<Window x:Class="TabuadaXaml.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TabuadaXaml"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        KeyDown="KeyDownWindow"
        Title="Tabuada" SizeToContent="WidthAndHeight">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="Painel"
                    Grid.Column="1"
                    HorizontalAlignment="Center"/>

        <StackPanel>
            <TextBlock Text="Numero:"/>
            <TextBox x:Name="txtNumero"/>
            <Button Content="Calcular"
                    Click="Calcular"/>
        </StackPanel>
    </Grid>
</Window>
